using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

public class GamesAlgorythm : MonoBehaviour {

    static public event Action NodeAnalyzedEvent;
    static public event Action NodeSkippedEvent;

    static public bool IsSearching = false;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
